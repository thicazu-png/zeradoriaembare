name: Lovable Auto Deploy

on:
  push:
    branches:
      - main

jobs:
  ping-lovable:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Signal Deploy
        run: echo "Código enviado para a main. Verificando deploy no Lovable..."
